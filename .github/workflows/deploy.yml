name: Build, push, deploy (Render + GHCR)

on:
  push:
    branches: ["main"]
  workflow_dispatch:
    inputs:
      rebuild_all:
        description: "Build/push all backend images and trigger Render deploy hooks (useful for testing)"
        type: boolean
        default: false
      skip_render:
        description: "Only build/push images to GHCR; do not call Render deploy hooks"
        type: boolean
        default: false

permissions:
  contents: read
  packages: write
  # Used by buildx gha cache (type=gha)
  actions: write

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_push_deploy:
    runs-on: ubuntu-latest
    env:
      IMAGE_TAG: ${{ github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Helps path filters on push events (comparing to prior commit).
          fetch-depth: 0

      - name: Detect changed paths
        id: changes
        uses: dorny/paths-filter@v3
        with:
          filters: |
            workflows:
              - '.github/workflows/**'
            api:
              - 'api/**'
            simulator:
              - 'device-simulator/**'

      - name: Compute lowercase GHCR owner (Docker requires lowercase)
        id: meta
        shell: bash
        run: |
          owner_lc="$(echo "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')"
          echo "ghcr_owner_lc=${owner_lc}" >> "${GITHUB_OUTPUT}"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push api
        if: |
          (github.event_name == 'workflow_dispatch' && inputs.rebuild_all == 'true') ||
          steps.changes.outputs.api == 'true' ||
          steps.changes.outputs.workflows == 'true'
        uses: docker/build-push-action@v6
        with:
          context: ./api
          file: ./api/Dockerfile
          push: true
          provenance: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ghcr.io/${{ steps.meta.outputs.ghcr_owner_lc }}/signalguard-api:${{ env.IMAGE_TAG }}
            ghcr.io/${{ steps.meta.outputs.ghcr_owner_lc }}/signalguard-api:latest

      # detection-service is now merged into the API process (api/detector.py)

      - name: Build & push device-simulator
        if: |
          (github.event_name == 'workflow_dispatch' && inputs.rebuild_all == 'true') ||
          steps.changes.outputs.simulator == 'true' ||
          steps.changes.outputs.workflows == 'true'
        uses: docker/build-push-action@v6
        with:
          context: ./device-simulator
          file: ./device-simulator/Dockerfile
          push: true
          provenance: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ghcr.io/${{ steps.meta.outputs.ghcr_owner_lc }}/signalguard-device-simulator:${{ env.IMAGE_TAG }}
            ghcr.io/${{ steps.meta.outputs.ghcr_owner_lc }}/signalguard-device-simulator:latest

      - name: Trigger Render deploy (api)
        if: |
          (github.event_name != 'workflow_dispatch' || inputs.skip_render != 'true') &&
          (
            (github.event_name == 'workflow_dispatch' && inputs.rebuild_all == 'true') ||
            steps.changes.outputs.api == 'true' ||
            steps.changes.outputs.workflows == 'true'
          )
        env:
          RENDER_DEPLOY_HOOK: ${{ secrets.RENDER_DEPLOY_HOOK_API }}
        run: |
          if [ -z "${RENDER_DEPLOY_HOOK}" ]; then
            echo "RENDER_DEPLOY_HOOK_API not set; skipping"
            exit 0
          fi
          curl -fsSL -X POST "${RENDER_DEPLOY_HOOK}"

      # Render deploy hook for detector removed — now co-located in the API

      - name: Trigger Render deploy (simulator worker)
        if: |
          (github.event_name != 'workflow_dispatch' || inputs.skip_render != 'true') &&
          (
            (github.event_name == 'workflow_dispatch' && inputs.rebuild_all == 'true') ||
            steps.changes.outputs.simulator == 'true' ||
            steps.changes.outputs.workflows == 'true'
          )
        env:
          RENDER_DEPLOY_HOOK: ${{ secrets.RENDER_DEPLOY_HOOK_SIMULATOR }}
        run: |
          if [ -z "${RENDER_DEPLOY_HOOK}" ]; then
            echo "RENDER_DEPLOY_HOOK_SIMULATOR not set; skipping"
            exit 0
          fi
          curl -fsSL -X POST "${RENDER_DEPLOY_HOOK}"
